[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kareus"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "torch",
]

[tool.setuptools.packages.find]
include = ["kareus*"]
